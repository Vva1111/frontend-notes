**Test Issue**
- [ ] 운영 이력 등 다운로드 기능이 있는 화면에서는 테이블에만 있는 데이터를 다운로드 하고 있는데, 현재는 테이블에 데이터를 버추얼 스크롤로 전체 데이터의 일부분만 나타내고 있음
	1. 테이블 데이터에 최대 1000개 까지의 데이터를 표현하고, 다운로드도 1000개까지
		( 백엔드 최대 요청 건수도 1000개 까지임 )
	2. 현재 방식으로 약 100개 정도의 데이터만 계속 불러오고, 다운로드 할 때만 더 많은 데이터를 다운로드 하게 하려면, 백엔드에 다운로드를 요청하는 방식이 필요할 것으로 보임
		( 1000개 이상의 데이터를 다운받기 위해서도 백엔드에 다운로드 요청이 필요할 듯 )
- [ ] 이벤트
	- [ ] 현재 알람
		- [ ] 조회 대상(node/vm) 선택 후 전체로 바꿨을 때, 심각도 options 초기화 필요
			- [ ] 기본 심각도 값은 있고, 이후 대상이나 지표 바꿨을 때 그에 맞는 심각도 옵션으로 주입으로 방식 변경 필요


**수정 필요**
- [x] 성능 통계, 리포트 대상 선택 트리에서 하위 목록이 없는 클러스터 혹은 노드 or vm 을 선택했을 때, 항목 구분 없이 한꺼번에 요청되는 문제.
	- [x] 아이디가 들어올 때, node 혹은 vm 인지 구분해서 요청 필요
- [x] 리포트, 미터링, 장애 통계 테이블 버추얼 스크롤 데이터 get api 연동 필요
- [x] DetilSerachComp 리미트 1년 설정 다시 확인 필요


**SLA**
- [x] 하나의 범위 값 최댓값 입력 -> 다음 범위 값 placeholder 에 소수점 둘째 자리 + 0.01 한 값으로 입력 가이드 적용
- [x] 팝업 말고 범위 인풋으로
- [x] 지정하지 않은 값이 있으면 에러, 모든 값 필수 값
- [ ] Node - 시스템 부하율 : 최대값 200까지 허용할 수 있도록 설정
- [ ] 타입 인터페이스 재정의
- [x] (컨펌 / 협의 필요) EditDavisSlaView 내부를 div 가 아닌 테이블 형태로 만들어야 할 듯?? ( 디자인 최종 적용으로로 수정 필요 )
	- [x] div 에서 v-for 사용하지 않고 table이 생성된 컴포넌트를 expansion 에서 가져오는걸로
		- [x] 그러려면 지표 레벨 값을 따로 표시해야 하는데 모든 항목에서 지표 레벨 값이 같은가?
			- [x] node / vm 별로 지표값은 동일하게 간다.
			- [x] metrics/level api 로 node/vm 별 레벨값을 가져올 수 있다.
	- [ ] 테이블이 아닌 지표 별 컴포넌트 형식으로 변경 ( 디자인도 변경 )

**LOG**
- [x] api 이력 없애고 운영 이력은 지금 적용돼 있는 포탈에서 기존 제이드 걸로 변경

**보고서**
- [x] 스케쥴 기능을 제외 -> 성능 통계와 동일한 방식으로, 동일한 데이터를 가져오기 때문에 성능 통계와 통합 ( -> 기존 리포트의 기능을 수정하는 방식으로 재개발 )
- [ ] 보고서 일정은 시간 없이 연/월/일 만 보냄 - UTC 없이 ( 일, 주 ) - 요청자의 국가 정보가 있어야 하는데 지금은 한국만, 보낸다고 가정
	- [ ] 월은 사용자가 달력에서 월 기준 데이터를 원할 시, 달 만 선택 혹은 시작 1일, 종료 말일 강제 선택 둘 중 한 방법으로 적용해야 함
- [ ] 보고서는 기능 수정 후 살림
- [ ] 스케쥴 기능 없앰
- [ ] 과거의 데이터를 리포트로 변경
- [ ] - 보고서 요청  
    - 목록  
        - 보고서(메일) 발송 이력  
        - 열  
            - 제목 - 담당자 입력  
            - 대상 (복수 가능) - Node명, VM명  
            - 대상 타입 - Node, VM  
            - 기간 - 시작/종료 (년 월 일)  
            - 형식 - 일간/주간/월간  
            - 신청자 - 보고서 신청 담당자 성명(아이디)  
            - 수신자 메일 - 신청자(담당자) 메일 또는 수정한 메일  
            - 요청 일시 - 년 월 일 시 분 초  
            - 완료 일시 - 년 월 일 시 분 초 - 완료/실패 일시  
            - 발송 상태 - 생성 중/발송 중/완료/실패  
    - 입력 ( 보고서 생성 요청 파라미터)
        - 제목  
            - 사용자 입력 - 중복 허용  
	- 메일 전송 할 이메일 ( 기본값 - 사용자, 수정 가능하도록 )
	- 보고서 종류 
		- 대상 자원(node/vm)
		- 기간
			- 일/주/월 선택은 시작-종료 기간보다 먼저 선택
	            - 일간  
	            - 주간  
	                - 시작일부터 7일 간격 (요일 상관없음)  
	            - 월간 선택 시,
	                - 시작일을 1일로 설정, 종료일은 월력만 나타내거나 혹은 해당 월의 제일 마지막 날 강제 선택
	- 기간 - 시작 일시, 종료 일시  
	- 대상 자원 ids

**성능 통계**
- [ ] 테이블 데이터 -> 차트 데이터 변환은 web woker 고려. 연산이 오래 걸릴 경우에는 차트에만 적용되는 프로그레스 바 적용 검토 -> 일정 문제로 web woker 사용하지 않고 변환 우선 적용
- [x] 리포트 기능을 성능 통계로 통합 ( 데이터 가져오는 방식(주기 선택, 다중 대상 선택, 기간 적용 등)과 조회 결과가 리포트와 같음 )
	- [x] 성능 통계 조회 후 상단에 리포트 버튼 추가
		- [x] 리포트 버튼 클릭 시, 알림 창에서 사용자의 이메일로 리포트 전송 가능하도록 기능 추가 필요 ( 이메일은 기본은 사용자 이메일, 변경 가능 )
	- [ ] 리포트는 별개 기능으로 기능 수정만
- [ ] node / vm 선택 -> node 까지만 선택, node-vm 까지 선택
	- [ ] select 에 제한을 둘 것인지, select 에 제한을 두지 않고 표현 갯수에 제한을 둘 것인지 서버 성능 테스트 후 결정
- [x] (협의 필요) 성능 통계 조회를 위한 파라미터(node, vm 목록)를 가져오기 위한 api 없음, 월요일 협의 필요

**이벤트**
- [ ] (협의 필요) 기존 : 현재 알람, 이벤트 이력, 장애 통계인데, 현재 api 문서에 있는 이벤트는 다 다름

**이벤트**
- [ ] 검색 조건
	- [ ] severity, metric, type( node / vm )