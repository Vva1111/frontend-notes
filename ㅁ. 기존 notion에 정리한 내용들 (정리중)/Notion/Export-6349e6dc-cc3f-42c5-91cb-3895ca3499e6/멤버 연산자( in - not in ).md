- ==**`in`**== : 포함한다
    
    ex) print( ’c’ in ‘cat’ ) ⇒ c가 cat 안에 포함되어 있기 때문에 True를 반환한다
    
      
    

- ==**`not in`**== : 포함하지 않는다
    
    ex) print( ’c’ not in ‘cat’ ) ⇒ not in을 사용해 ‘c’가 ‘cat’ 안에 포함되지 않는다고 했지만,
    
    실제로는 포함이 되어있기 때문에 False값을 반환한다.