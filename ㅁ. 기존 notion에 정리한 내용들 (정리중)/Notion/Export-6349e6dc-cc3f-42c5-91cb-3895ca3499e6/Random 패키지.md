### Random 패키지를 사용하기 위해서는 import가 필요하다

- ==**`import random`**==

- ==**`from random import ****`**== ( Random 패키지 내에서 필요한 모듈만 사용 )
    
    - Random 패키지의 모듈
        
        - **`random.random()`** : 0 ~ 1 사이 무작위 값 생성
        
        - **`random.choice( a )`** : a 내부에서 1개의 값을 무작위 추출
        
        - **`random.choices( a, 5 )`** : a 내부에서 5개의 값을 무작위 추출
        
        - **`random.sample()` :**
        
        - **`random.shuffle( )` :**